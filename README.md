

## About running this project Laravel



set up .env like this for mysql connection:-

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=rtproject12
DB_USERNAME=root
DB_PASSWORD=


then run 

php artisan db:seed

for seeding the database

then run 

php artisan serve
